============
Installation
============

At the command line::

    $ easy_install django-history-actions

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-history-actions
    $ pip install django-history-actions
